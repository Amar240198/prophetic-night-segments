# React Native conceptual integration

Call `calculateNightSegments` with provider-normalised ISO instants. Pass chosen alarm instants to the platform notification library, request permission in context, and recalculate when the app becomes active, the timetable changes, or the device timezone changes. The engine itself requires no network access and must not be used as a substitute for the prayer-time provider.
